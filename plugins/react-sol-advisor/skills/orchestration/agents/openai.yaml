interface:
  display_name: "React Sol Advisor Orchestration"
  short_description: "Luna-first React delivery; escalate to Terra / High and fresh Sol review when needed"
  default_prompt: "Use $orchestration for React, Next.js, React Native, and Expo work. Default to economy (green -> Luna / Max; amber -> Sol decomposition with Luna subparts and Terra core; red -> Terra / High plus fresh Sol review). Balanced and critical policies are explicit alternatives. Emit a ROUTING DECISION with POLICY, RISK, LANE, OWNERSHIP, QUALITY CONTRACT, ESCALATION TRIGGERS, and PR POLICY before delegating. Do not silently substitute models, lanes, or tools. Optional Linear intake is read-only and never a hard dependency."
